// Mock data removed — all content is loaded from the backend API.
// These empty exports are kept so existing imports don't break.
import { NewsItem, Advertisement } from './types';

export const MOCK_NEWS: NewsItem[] = [];
export const MOCK_ADS: Advertisement[] = [];
