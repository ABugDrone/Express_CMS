version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file: backend/.env
    environment:
      - NODE_ENV=production
      - APP_URL=${APP_URL:-http://localhost:8000}
    volumes:
      - uploads:/app/uploads
    restart: unless-stopped

volumes:
  uploads:
